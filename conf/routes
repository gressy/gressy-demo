# Load the Gressy-Entities routes, so the panel can find them
->  /panel gressyEntities.Routes

# Other routes
GET /api/welcome gressy.controllers.HomeController.index()

# Assets
GET / controllers.Assets.at(path="/public", file="index.htm")
GET /*file controllers.Assets.at(path="/public", file)
